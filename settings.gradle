pluginManagement {
    repositories {
        mavenCentral()
        maven { url 'https://repo.spring.io/milestone' }
        gradlePluginPortal()
    }
    plugins {
        id 'org.liquibase.gradle' version "2.1.1"
    }
}

rootProject.name = 'institute'
